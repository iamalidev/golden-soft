import React from "react";
import * as Style from "./style";

const MobileMenu = ({ open }) => {
  return <Style.MobileMenuWrapper open={open}>alisher</Style.MobileMenuWrapper>;
};

export default MobileMenu;
